<div class="row">
  <div class="col-xs-8 col-xs-offset-2" style="padding-top : 50px">
    <%= simple_form_for [@restaurant, @review] do |f| %>
      <%= f.input :content %>
      <%= f.input :rating %>
      <%= f.button :submit %>
    <% end %>
  </div>
</div>
